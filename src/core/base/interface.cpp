#include "interface.h"

namespace fem::core::base {

// Pure virtual destructor implementation is in the header
// This file exists for future non-template interface implementations
// and to maintain consistent build structure

} // namespace fem::core